*.js
!jest.config.js
*.d.ts
node_modules

# CDK asset staging directory
.cdk.staging
cdk.out

# Access details with sensitive credentials
access_details/*
!access_details/.gitignore
!access_details/README.md

# Security directory (contains sensitive audit files with credentials)
security/
!security/README.md

# Binary tools (should be installed locally, not committed)
*.exe
*.zip
*.jar
kubeconfig

# Temporary build artifacts
tmp/

# Sensitive test files with hardcoded credentials
jenkins-test-jobs/

# Environment and credential files
.env
.env.local
.env.*.local
*.pem
*.key
*.crt
credentials
config

# AWS and Kubernetes sensitive files
*.kubeconfig
*kubeconfig*
.aws/
.kube/

# Secrets and passwords
secrets/
*.secret
*.password
