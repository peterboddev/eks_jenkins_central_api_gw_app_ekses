apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jenkins
  namespace: jenkins
  annotations:
    # ALB configuration
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/healthcheck-path: /login
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}]'
    
    # Tags for the ALB
    alb.ingress.kubernetes.io/tags: Environment=production,Application=jenkins
    
    # Subnets are auto-discovered based on tags:
    # - kubernetes.io/role/elb=1 (for public subnets)
    # - kubernetes.io/cluster/jenkins-eks-cluster=shared
    # No need to specify subnet IDs - ALB controller will find them automatically
    
    # Security: Access controlled via security group (managed by CDK)
    # Security group ID is injected by CDK during deployment
spec:
  ingressClassName: alb
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: jenkins
            port:
              number: 8080
